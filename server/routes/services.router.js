/**
 * @file routes/services.router.js
 * @description 서비스 관련 라우터
 * 251216 v1.0.0 Lee init
 */
import express from "express";

const router = express.Router();

// 여기에 서비스 관련 API 라우트를 추가합니다.
// 예: router.get('/', ...);

export default router;
